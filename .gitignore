.DS_Store

# Holds the real Gmail account identity (STEP 1.1 cross-check needs it).
# Structure and full allowlist are published as digest-routine/sources.example.json.
digest-routine/sources.json

# SDD scratch: briefs, reports, review packages. Review packages contain
# pre-redaction diffs, so this must never be committed.
.superpowers/
